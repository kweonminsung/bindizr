Bindizr stack has been installed.

API service:
  {{ include "bindizr-stack.fullname" . }}-api:{{ .Values.bindizr.api.service.port }}

Authoritative DNS service:
  {{ include "bindizr-stack.fullname" . }}-bind9:{{ .Values.bind9.service.dnsPort }} ({{ .Values.bind9.service.type }})

BIND9 replicas are exposed for authoritative DNS traffic.

Before production use:
  - Set bindizr.image.repository and bindizr.image.tag if you need a different Bindizr image.
  - nsupdate TSIG keys and per-zone policies are managed at runtime via the bindizr CLI or HTTP API.
  - BIND9 accepts NOTIFY from any source by default through allow-notify { any; }.
  - Set bindizr.database.existingSecret or bindizr.database.serverUrl when using mysql/postgresql.
