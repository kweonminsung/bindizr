# Helm values for the kind-cluster.yaml test cluster. Mirrors the
# docker-compose.test.yml settings (bundled postgres, open API, unsigned
# nsupdate).
#
# Usage:
#   kind create cluster --config kind-cluster.yaml
#   helm install bindizr charts -f kind-values.yaml
#   # Pin the bind9 NodePort to the port kind-cluster.yaml maps to the host
#   # (the chart does not expose nodePort in values):
#   kubectl patch svc bindizr-bind9 --type merge -p '{"spec":{"ports":[{"name":"dns-tcp","port":53,"targetPort":"dns-tcp","protocol":"TCP","nodePort":30053},{"name":"dns-udp","port":53,"targetPort":"dns-udp","protocol":"UDP","nodePort":30053}]}}'
#   dig -p 5300 @127.0.0.1 <zone> SOA

fullnameOverride: bindizr

bindizr:
  api:
    requireAuthentication: false
  dns:
    nsupdateAllowUnsigned: true
  database:
    type: postgresql

bind9:
  service:
    # kind has no cloud LB; reach bind9 through the pinned NodePort instead.
    type: NodePort

postgresql:
  enabled: true
