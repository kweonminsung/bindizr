mod common;

mod api {
    mod health;
    mod notify;
    mod record;
    mod tsig_key;
    mod zone;
}

mod cli {
    mod config;
    mod daemon;
    mod doctor;
    mod record;
    mod tsig_key;
    mod zone;
}
