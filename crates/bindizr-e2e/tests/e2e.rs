mod common;

mod api {
    pub(crate) mod record;
    pub(crate) mod tsig_key;
    pub(crate) mod zone;
}

mod cli {
    pub(crate) mod record;
    pub(crate) mod tsig_key;
    pub(crate) mod zone;
}
