options {
    // On the bind9-data volume, so a restarted task answers before its first transfer.
    directory "/var/lib/bind";
    listen-on port 53 { any; };
    listen-on-v6 port 53 { any; };
    recursion no;
    allow-query { any; };
    allow-notify { any; };

    ixfr-from-differences yes;
    catalog-zones {
        zone "catalog.bindizr" default-primaries { __BINDIZR_PRIMARY__ port 53; };
    };
};

zone "catalog.bindizr" {
    type secondary;
    primaries { __BINDIZR_PRIMARY__ port 53; };
    file "/var/lib/bind/catalog.bindizr.zone";
    ixfr-from-differences yes;
};
